{
  "name": "zuivellogboek-het-boerderijwinkeltje",
  "version": "1.0.0",
  "private": true,
  "description": "Serverversie van het zuivellogboek voor Het Boerderijwinkeltje.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js && node --check service-worker.js"
  },
  "engines": {
    "node": ">=20"
  }
}
